# Animated Header

This component is targeted for usage of SVG:s exported from SVGator and uses its player for interaction.

Expected behavior is:

1. It plays the animation on load
2. It scrolls the user down to the title when it finished playing.
